# TravelGo Backend

## Run locally
1. `npm install`
2. Copy `.env.example` to `.env`
3. Add Azure Storage and Cosmos DB values
4. `npm start`

## API endpoints
- GET `/api/health`
- POST `/api/media`
- GET `/api/media`
- GET `/api/media/:id`
- PUT `/api/media/:id`
- DELETE `/api/media/:id`
